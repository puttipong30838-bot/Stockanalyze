{
  "name": "stockpulse",
  "private": true,
  "workspaces": [
    "server",
    "app"
  ],
  "scripts": {
    "server:dev": "npm run dev --workspace=server",
    "server:test": "npm run test --workspace=server",
    "app:web": "npm run web --workspace=app"
  }
}
