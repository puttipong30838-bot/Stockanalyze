# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

Pinned to SDK 56 (not the newest 57) because the current App Store release of
Expo Go had not yet added SDK 57 support when this was tested — see git log
for details. Bump back to 57 once Expo Go's store listing catches up.
